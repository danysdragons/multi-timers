Use the attached desktop Multi Timers screenshot as the exact visual design reference. Create its responsive mobile web application layout as one flat straight-on screenshot, 1024 by 1536 pixels, edge to edge. This is the same application and same data at a mobile viewport, not a photo of a phone, not a browser/device frame, and not a presentation board. Preserve the desktop palette, typography character, icons, subtle borders, corner radii, and task names and durations. Reflow the interface into a natural single column with generous touch targets and crisp large readable text. The 1024-pixel image represents a roughly 430 CSS-pixel mobile viewport at high resolution; scale text and controls accordingly.

Visual invariants: warm ivory background, deep forest green active panel, white list surfaces, muted sage running indicators, charcoal text, clean humanist sans-serif and monospaced timer numerals. No gradients, charts, photos, progress rings, or illustrations.

Compact top bar: green stopwatch logo and "Multi Timers" on the left; settings gear on the right. Underneath: "Today" as selected navigation and "Task library" as the other option. Then page heading "Today", small date "Fri, Sep 25, 2026", previous/next date arrows aligned to the right.

Dominant full-width dark forest green active-task card in the upper third: small mint dot with "RUNNING", task name "Study X", large white cumulative duration "01:03:42" with the label "TOTAL TODAY". Below it smaller "Current session 00:18:42". One full-width ivory "Stop timer" button with a square stop icon. Keep the entire card compact enough to leave substantial room for the task list.

Section heading "Today's tasks" and a green "+ Add" button. A single white bordered task list with exactly three horizontal rows. Each row uses two lines rather than desktop columns. Row 1: checkbox for selection, task "Study X", sage status "Running", monospaced "01:03:42", secondary "2 sessions", compact "Stop" button. Row 2: checkbox, task "Study Y", status "Paused", "00:35:00", "1 session", "Resume" button with a play icon. Row 3: checkbox, task "Reading", status "Done" with a checkmark, "00:20:00", "1 session", "Resume" button. Keep all three rows legible and comfortably spaced, with status text, durations, and buttons never overlapping. Only Study X is running. The selection checkboxes must be distinct from the completion status.

Below list: outlined "Add favorites" action and a full-width outlined "Copy selected to tomorrow" button. Footer: small "Saved on this device" and "Back up data" link. Fit the meaningful interface in the image without awkward empty space, clipping, or microscopic text. Do not add account, sync, reporting, or countdown features. Preserve the desktop product's quiet, refined visual identity.
